@{
    ViewData["Title"] = "Acceso denegado";
    Layout = "_AuthLayout";
}

<div class="noc-card">
    <h1 class="h4">Acceso denegado</h1>
    <p class="text-secondary">No tienes permisos para acceder a esta sección.</p>
    <a class="btn btn-outline-light" asp-controller="Account" asp-action="Login">Volver</a>
</div>