Última microcorrección, sin tocar nada más:

En la política api del rate limiter, después de autenticación usa el claim/identificador estable api_key_id para API keys en vez de volver a leer y hashear X-Api-Key. Para usuario humano usa su identificador estable; IP sólo como fallback anónimo. Nunca key cruda.
Corrige el comentario de Data Protection que todavía dice que Staging permite keys en claro. Debe decir que sólo Development/Testing son permisivos y Staging/Production/otros fallan cerrado.
Ejecuta Unit + Integration + Runtime + E2E una última vez con la DB _test.
Si todo pasa, actualiza FINAL_HOSTILE_AUDIT.md, commit y push sólo a codex/atlasnoc-review-20260909. No merge ni PR.
Reporta SHA final y conteos Passed/Failed/Skipped.